export type ChartId = "expenditures-over-time";

export const CHARTS: { id: ChartId; label: string }[] = [
  { id: "expenditures-over-time", label: "Expenditures over time" },
];

export const DEFAULT_CHART: ChartId = "expenditures-over-time";
